#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s[100005][2];
    int n;
    cin >> n >> s[0][0] >> s[0][1];
    for(i=1; i<n; i++){
        cin >> s[i][0] >> s[i][1];
        if(s[i][0] >=s[i][1]){

        }
        else
    }
}
