#include <bits/stdc++.h>
using namespace std;

#define PI  acos(-1)
#define SIZ  100005
#define max3(a,b,c)  max(max(a,b),c)
#define max4(a,b,c,d)  max(max(a,b), max(c,d))
#define TAKEINP(ara)  for(i=0; i<n; i++) cin >> ara[i]
#define FOR(s,n,p)  for(i=s; i<n; i+=p)
#define push_back  psb
#define pop_back  pob
#define ll  long long

int main()
{

    string s;
    char str[100];

    for(int i=0; i<3; i++) cin >> s, str[i] = s;
    for(int i=0; i<3; i++) cout << str[i] << endl;
    return 0;
}
