#include <bits/stdc++.h>
#define MAX(a,b) (a>b)?a:b
#define FOR(s,f,k) for(i=s; i<f; i+=k)
#define INARA for(i=0; i<n; i++) cin >> ara[i]
#define PI acos(-1)
#define ll long long
using namespace std;
int main()
{
    int x, y, t;
    cin >> x >> y;
    cout << MAX(x,y);
    return 0;
}

